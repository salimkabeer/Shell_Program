# Arithmetic operators
# +, -, *, /, %, =, ==, !=

# Relational operators
# -eq, -ne, -gt, -lt, -ge, -le

# Boolean operators
# !(not), -a(AND), -o(OR)

# String operators
# =(equality), !=(not equal string), -z(zero length string), -n(non zero string)
